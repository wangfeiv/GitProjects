- Rebuilt the `duoduo-study` skill under `skills/duoduo-study/` using `skill-creator` patterns. The skill now manages Duoduo's wrong-question bank across math/chinese/english, including text/image intake, knowledge tracking, review plans, mastery updates, weak-point queries, same-type exercise generation, and Word review paper generation.
- Created persistent Duoduo study data layout under `duoduo/` with initialized templates for each subject: `errors.md`, `knowledge.md`, `review.md`, plus `assets/{math,chinese,english}/`.
- Standardized table schemas for Duoduo data. `errors.md` summary tables now include: #, 日期, 来源, 题目类型, 知识点, 难度, 优先级, 状态, 下次复习日期, 关联图片. `knowledge.md` tracks knowledge point mastery and linked wrong questions. `review.md` keeps current review items plus appended historical plans.
- Added reusable `duoduo-study` scripts:
  - `scripts/generate_review_docx.py`: generate pure-text docx review papers with sections, difficulty labels, answers, and explanations, while sanitizing text to reduce Word garbling.
  - `scripts/build_review_json.py`: parse `errors.md`, filter by status/priority, group by question type, and emit JSON for the docx generator.
  - `scripts/build_review_docx_from_errors.py`: one-command pipeline from `errors.md` to docx review paper.
  - `scripts/build_variant_practice_seed_json.py`: build seed JSON for same-type variant practice generation from stored wrong questions.
- Added `duoduo-study` references: `storage-schema.md`, `word-generation.md`, `closed-loop-workflow.md`, `one-command-workflow.md`, and `variant-practice-workflow.md`.
- `duoduo-study` was validated successfully with the skill validator after the rebuild/enhancements.
